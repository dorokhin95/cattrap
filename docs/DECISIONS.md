# Журнал архитектурных решений (ADR) — CatTrap

## ADR-001: Выбор технологического стека Phaser 3 + TypeScript + Vite
- **Контекст:** Требуется 2D trap-платформер с честной физикой, адаптивной камерой, fixed timestep 60 FPS, сценами и поддержкой Web / Telegram Mini App.
- **Решение:** Использовать Phaser 3 в связке с TypeScript и Vite.
- **Обоснование:** Phaser 3 предоставляет готовые механизмы Arcade Physics с фиксированным шагом (`fixedStep`), сцены, тайлмапы, эмиттеры частиц, развитую систему камер и событий, идеально подходя под требования ТЗ (раздел 54).

---

## ADR-002: Процедурный синтез звуков через WebAudio API
- **Контекст:** Игра должна быть максимально лёгкой для Telegram Mini App (< 4–5 МБ) и гарантировать мгновенный запуск без ожидания загрузки аудиофайлов.
- **Решение:** Процедурный синтез ретро-звуковых эффектов (квадратные, треугольные волны, шум) и фонового chiptune-лупа непосредственно в WebAudio API.
- **Обоснование:** Нулевой вес аудиоассетов, отсутствие сетевых задержек, отсутствие проблем с блокировкой CORS, мгновенная реакция при взаимодействии.

---

## ADR-003: Процедурная генерация пиксель-арта на Canvas
- **Контекст:** Кот должен иметь характерный силуэт (крупная голова, уши с выемкой, хвост-вопросительный знак с тёмным кончиком, ошейник, светлая мордочка) и плавно анимироваться (Idle, Run, Jump, Fall, Landing, Death, Portal).
- **Решение:** Процедурный генератор текстур `PixelArtGenerator`, создающий растровые пиксель-арт полотна на HTML5 Canvas при старте в `BootScene` и передающий их в Phaser TextureManager.
- **Обоснование:** Автономность, отсутствие битых картинок, возможность масштабирования с `pixelArt: true`, мгновенная загрузка.

---

## ADR-004: Детерминированная модель триггеров ловушек
- **Контекст:** ТЗ строго запрещает рандом и непредсказуемость. Игрок должен стабильно проходить уровень, выучив принцип ловушки.
- **Решение:** Все триггеры базируются на дискретных координатах игрока (`player_x`, `player_y`), событиях касания (`onContact`) или последовательных очередях с фиксированным интервалом (`delay: 120ms`).
- **Обоснование:** 100% повторяемость условий гибели и победы вне зависимости от частоты кадров.

---

## ADR-005: Уменьшенный Lethal Hitbox опасных объектов
- **Контекст:** Касание крайнего пикселя ловушки не должно приводить к гибели котика.
- **Решение:** Смертельный коллайдер шипов и падающих блоков составляет 65–75% от их визуальной площади, а физический коллайдер котика — 14×19 px при размере спрайта 24×24 px.
- **Обоснование:** Исключает ощущение несправедливой гибели («пиксель-перфект»).

---

## ADR-006: Единая кодовая база для Web и Telegram Mini App
- **Контекст:** Требуется поддержка GitHub Pages, мобильных/десктопных браузеров и Telegram Mini App.
- **Решение:** Слой `PlatformService` (`BrowserPlatformService` и `TelegramPlatformService`) с полиморфным интерфейсом.
- **Обоснование:** Устраняет дублирование кода, гарантируя одинаковое поведение физики и уровней на всех платформах.

---

## ADR-007: Полноэкранный двухзонный сенсорный ввод (Dual-Zone Touch Controller)
- **Контекст:** На мобильных устройствах игроки играют двумя большими пальцами на весу. Привязка к маленьким кнопкам приводила к тому, что малейшее смещение подушечки пальца срывало прыжок или бег кота. Кроме того, внутреннее смещение `displayOriginX/Y` в Phaser 3 обрезало локальные хитбоксы.
- **Решение:**
  1. Переход на глобальный двухзонный перехват (`Dual-Zone Touch`):
     - Вся левая половина экрана — непрерывный D-Pad со скольжением (`pointermove`) и расчетом направления относительно центра;
     - Вся правая половина экрана — 100% безотказный прыжок при касании в любой точке;
     - Верхняя полоса экрана изолирована для кнопки паузы `⏸` и HUD.
  2. В `main.ts` и `style.css` установлены `activePointers: 4`, `touch: { capture: true }`, строгие CSS-правила `overscroll-behavior: none !important; touch-action: none !important;` и отключено системное контекстное меню `contextmenu`.
  3. Буфер ввода прыжка увеличен до 130 мс, а Coyote Time — до 110 мс.
- **Обоснование:** Гарантирует безупречный, плавный и безотказный отклик на любых сенсорных экранах (iOS Safari, Android Chrome, Telegram WebApp) без необходимости выцеливать пиксели кнопок.

---

## ADR-008: Прозрачный ресайз при повороте экрана (без модального окна)
- **Контекст:** При смене ориентации на мобильных устройствах (iOS Safari) игра зависала: `rotateModal` паузировал игру и отключал тачконтролы (`setEnabled(false)`), но iOS обновляет `window.innerWidth/Height` с задержкой анимации (~300 мс), из-за чего `resize`-событие приходило позже — модалка появлялась, кнопка «Продолжить» не реагировала на касания, зависая намертво.
- **Решение:**
  1. Полное удаление `rotateModal` и `previousOrientationMode` из `UIScene`.
  2. В `main.ts`: `resizeGame()` вызывается через `window.resize` + тройной `setTimeout` (50/150/300 мс) по `orientationchange` + `ResizeObserver` на `#game-container`.
  3. В `style.css`: `#game-container` переведён на `position: fixed; height: 100dvh` — браузер не задерживает пересчёт layout при повороте.
  4. В каждой сцене (`GameScene`, `UIScene`, `MenuScene`) и `CameraSystem`: в `handleResize` явный вызов `camera.setViewport(0,0,w,h)` + `camera.setSize(w,h)`, т.к. Phaser 3 в режиме `RESIZE` не обновляет камеры автоматически.
- **Обоснование:** Устраняет зависание без потери игрового контекста; поворот экрана происходит незаметно для игрока без паузы и без блокировки ввода.

---

## ADR-009: Нулевые визуальные подсказки для скрытых ловушек (Zero-Hint Trap Design)
- **Контекст:** В ранних прототипах и ТЗ предлагались «микро-отличия» скрытых ловушек (едва заметный сдвиг шва на плитке `tile_fake`, тонкая щель на плите `pop_spike_floor`, трещина до касания на `tile_crumble`, оранжевые пиксели предупреждения на `falling_block`, видимые неоновые маркеры зон управления `ControlZone`, декоративные лапки на карнизах). Это превращало игру в монотонный пиксель-хантинг с медленным разглядыванием текстур вместо динамичного trap-платформера.
- **Решение:**
  1. Зафиксировать манифест: скрытые ловушки до момента активации визуально 100% идентичны нормальным блокам окружения.
  2. В `PixelArtGenerator`: убрать все сдвиги швов, щели и цветные маркеры. Скрытые ловушки генерируют текстуру, идентичную монолитному блоку `tile_solid`.
  3. `CrumbleBlock` стартует с текстурой `tile_solid` и проявляет трещину `tile_crumble` только после наступления котика.
  4. `ControlZone` (реверс и автобег) полностью невидима до входа котика; фидбек (`↔`, `>>`) выводится кратковременно над головой котика только в момент активации.
  5. В уровнях удалены декоративные подсказки (например, тайлы `paw` в Level 06 заменены на обычные блоки).
- **Обоснование:** Поддерживает ключевой игровой цикл CatTrap: *"всё выглядит безопасно → естественное действие игрока → внезапное нарушение ожидания → гибель → понимание («Ах ты сука 😄») → пространственное запоминание → прохождение знанием"*.
